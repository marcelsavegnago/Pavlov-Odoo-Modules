<odoo>
  <!-- List View -->
  <record model="ir.ui.view" id="project_enhancement_scrum_task_view_tree" >
    <field name="name">project.task.enhancement.scrum.tree</field>
    <field name="model">project.task</field>
    <field name="inherit_id" ref="project.view_task_tree2"/>
    <field eval="2" name="priority"/>
    <field name="arch" type="xml">
      <field name="name" position="before">
        <field name="task_number"
               readonly="1"/>
      </field>
    </field>
  </record>

  <!-- Form View -->
  <record model="ir.ui.view" id="project_enhancement_scrum_task_view_form" >
    <field name="name">project.task.enhancement.scrum.form</field>
    <field name="model">project.task</field>
    <field name="inherit_id" ref="project.view_task_form2"/>
    <field eval="2" name="priority"/>
    <field name="arch" type="xml">
      <data>
        <xpath expr="//field[@name='kanban_state']" position = "before">
          <field name="issue_type_image"
                 widget="image"
                 options="{&quot;size&quot;: [20, 20]}"/>
          <field name="task_number"
                 readonly="1"/>
        </xpath>
        <xpath expr="//field[@name='user_id']" position="after">
          <field name="planned_hours"/>
        </xpath>
        <xpath expr="//notebook" position="before">
          <field name="use_scrum"
                 invisible="1"/>
          <field name="allow_forecast"
                 invisible="1"/>
          <group attrs="{'invisible':[('use_scrum','=', False)]}">
            <field name="acceptance_criteria"/>
          </group>
          <group colspan="2" col="4" attrs="{'invisible':[('use_scrum','=', False)]}">
            <field name="issue_type"
                   widget="selection"
                   attrs="{'required':[('use_scrum','=', True)]}"/>
            <field name="epic_id"
                   widget="selection"
                   domain="[('project_id','=',project_id)]"/>
            <field name="source"
                   widget="selection"
                   attrs="{'required':[('use_scrum','=', True)]}"/>
            <field name="fix_versions"
                   widget="many2many_tags"
                   options="{'color_field': 'color', 'no_create_edit': True}"
                   domain="[('project_id','=', project_id),('status','=','unreleased')]"/>
            <field name="story_point_estimate"
                   widget="selection"/>
            <field name="affects_versions"
                   widget="many2many_tags"
                   options="{'color_field': 'color', 'no_create_edit': True}"
                   domain="[('project_id','=', project_id),('status','=','released')]"/>
            <field name="sprint_id"
                   widget="selection"
                   domain="[('status','!=', 'closed')]"/>
            <field name="categories"
                   widget="many2many_tags"
                   options="{'color_field': 'color', 'no_create_edit': True}"/>
            <field name="planned_hours"/>
            <field name="reporter"/>
          </group>
          <group attrs="{'invisible':[('use_scrum','=', False)]}">
            <field name="blocking_tasks"
                   widget="many2many">
              <tree>
                <field name="issue_type"/>
                <field name="name"/>
                <field name="user_id"/>
                <field name="story_point_estimate"/>
                <field name="sprint_id"/>
                <field name="stage_id"/>
                <field name="progress"
                       widget="progressbar"/>
                <button name="open_rec"
                        string="Open"
                        type="object"/>
              </tree>
            </field>
          </group>
        </xpath>
      </data>
    </field>
  </record>

  <!-- Kanban View -->
  <record model="ir.ui.view" id="project_enhancement_scrum_task_view_kanban">
    <field name="name">project.task.enhancement.scrum.kanban</field>
    <field name="model">project.task</field>
    <field name="inherit_id" ref="project.view_task_kanban"/>
    <field name="arch" type="xml">
      <data>
        <xpath expr="//div[@class='o_kanban_record_body']" position="after">
          <field name="sprint_id"
                 invisible="1"/>
          <field name="task_number"
                 invisible="1"/>
          <field name="story_point_estimate"
                 invisible="1"/>
          <field name="issue_type"
                 invisible="1"/>
          <field name="subtask_count"
                 invisible="1"/>
            <t t-if="record.sprint_id.raw_value">
              <span class="o_value"><field name="sprint_id"/></span>
            </t>
            <t t-if="record.story_point_estimate.raw_value">
             <span class="o_value">(<field name="story_point_estimate"/>)</span>
           </t>
           <div>
              <field name="fix_versions"
                     widget="many2many_tags"
                     options="{'color_field': 'color'}"/>
           </div>
        </xpath>
        <xpath expr="//div/field[@name='priority']" position="before">
          <t t-if="record.task_number.raw_value">
           <span class="o_value"><field name="task_number"/></span>
           <t t-if="record.subtask_count.raw_value > 0">
             <a class="o_project_kanban_box" name="action_subtask" type="object">
               <span class="o_value">(<t t-esc="record.subtask_count.value"/>)</span>
             </a>
           </t>
           <t t-if="record.issue_type.raw_value">
             <img t-att-src="kanban_image('project.scrum_issuetype', 'issue_type_image', record.issue_type.raw_value)" t-att-title="record.issue_type.value" t-att-alt="record.issue_type.value" width="18" height="18" class="oe_kanban_avatar"/>
           </t>
         </t>
        </xpath>
      </data>
    </field>
  </record>

  <!-- Search View -->
  <record model="ir.ui.view" id="project_enhancement_scrum_task_view_search">
    <field name="name">project.task.enhancement.scrum.search</field>
    <field name="model">project.task</field>
    <field name="inherit_id" ref="project.view_task_search_form"/>
    <field name="arch" type="xml">
      <field name="user_id" position="after">
        <field name="task_number"/>
        <field name="sprint_id"/>
        <field name="epic_id"/>
      </field>
      <filter name="Stage" position="after">
        <separator/>
        <filter string="Epics"
                name="epics"
                context="{'group_by':'epic_id'}"/>
      </filter>
    </field>
  </record>

</odoo>
