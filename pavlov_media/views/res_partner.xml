<?xml version="1.0"?>
<odoo>

    <!-- Show the helpdesk ticket smart button if count is 0 -->
    <record id="res_partner_ticket" model="ir.ui.view">
        <field name="name">res.partner.helpdesk.ticket</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="helpdesk.view_partner_form_inherit_helpdesk"/>
        <field name="arch" type="xml">
            <button name="action_open_helpdesk_ticket" position="attributes">
                <attribute name="attrs"/>
            </button>
        </field>
    </record>

</odoo>
