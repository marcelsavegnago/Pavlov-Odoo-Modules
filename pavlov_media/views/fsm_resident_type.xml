<odoo>
    <!-- Sequences-->
    <record id="fsm_resident_type_seq" model="ir.sequence">
      <field name="name">FSM Resident Type Sequence</field>
      <field name="code">fsm.resident_type</field>
      <field name="prefix">0</field>
      <field name="padding">1</field>
    </record>

    <!-- List View-->
    <record id="fsm_resident_type_view_list" model="ir.ui.view">
      <field name="name">FSM Resident Type List View</field>
      <field name="model">fsm.resident_type</field>
      <field name="arch" type="xml">
        <tree editable="top">
          <field name="sequence"
                 widget="handle"/>
          <field name="name"/>
          <field name="description"/>
        </tree>
      </field>
    </record>

    <!-- Form View -->
    <record id="fsm_resident_type_view_form" model="ir.ui.view">
      <field name="name">FSM Resident Type Form</field>
      <field name="model">fsm.resident_type</field>
      <field name="arch" type="xml">
        <form string="BW Change speed">
          <sheet>
            <div class="oe_title">
              <label for="name" class="oe_edit_only" string="Name" />
              <h1><field name="name" /></h1>
            </div>
            <group>
              <field name="description"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- Actions opening views on models -->
    <record id="fsm_resident_type_action" model="ir.actions.act_window">
      <field name="name">Resident Type Action</field>
      <field name="res_model">fsm.resident_type</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- Menu Items -->
    <menuitem
      name="Resident Types"
      id="fsm_resident_type_menu"
      parent="fieldservice.menu_fsm_config_location"
      action="fsm_resident_type_action"
      sequence="100"/>
</odoo>
